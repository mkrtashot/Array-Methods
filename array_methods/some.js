"use strict";

let arr = [1, 2, 3, 4, 5];
function someFake(arr) {
  for (let i = 0; i < arr.length; i++) {
    if (arr[i] % 2 === 0) {
      return true;
    }
  }
  return fake;
}
console.log(someFake(arr));
